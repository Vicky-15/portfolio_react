// with path, title of tab changed
export const setTitle = (path) => {
  if (path === "") return "Vignesh | Web Developer";
  if (path === "about") return "About Me | Web Developer";
  if (path === "work") return "My Works | Web Developer";
  // if (path === "contact") return "Contact | Web Developer";
};
